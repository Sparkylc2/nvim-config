-- Jupyter/REPL support (jupytext.nvim + vim-slime + vim-slime-cells) was removed
-- on 2 Sep 2026: the integration was not good enough to keep. If notebook work
-- comes back, molten-nvim is the modern option and replaces all three with
-- inline output. matlab (vim-matlab + matlab_ls) was dropped at the same time.

return {}
